import { motion } from "framer-motion";

export default function HelloJ12() {
  return (
    <motion.h1
      initial={{ opacity: 0, y: 50 }}
      animate={{ opacity: 1, y: 0 }}
      transition={{ duration: 1, ease: "easeOut" }}
      style={{
        fontSize: "3rem",
        textAlign: "center",
        marginTop: "20vh",
        color: "#ff6347", // tomato color
        fontFamily: "Arial, sans-serif",
      }}
    >
      HELLO J12 PEEPS!
    </motion.h1>
  );
}
